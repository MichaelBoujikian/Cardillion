# `src/prototype` - throwaway experiments

Visual/UX prototypes that answer a question and are then deleted. Nothing here is imported by
the real game. Current: the **look prototype** (milestone M1) that decides whether web +
three.js hits the Inscryption-grade visual bar.
