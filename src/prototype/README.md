# `src/prototype` - throwaway experiments

Visual/UX prototypes that answer a question and are then deleted. Nothing here is imported by
the real game. The M1 look prototype lived here until the battle scene in `src/render/battle`
superseded it (its environment code was promoted there).
